# Backend URL. Defaults to http://localhost:8000/api/v1 if unset.
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Set to "true" to force mock data even with a backend URL configured
# (useful for working on the UI without the FastAPI server running).
VITE_USE_MOCK=false
