# Root requirements: ML pipeline (notebooks + src/) — data prep, training,
# and the conformity pipeline. For the FastAPI backend alone, use
# api/requirements.txt (lighter, mock mode needs none of the heavy deps below).

# --- Core CV / PDF ---
opencv-python==4.10.0.84
pymupdf==1.24.10
numpy==1.26.4

# --- Deep learning (U-Net segmentation) ---
torch==2.4.1
segmentation-models-pytorch==0.3.4
albumentations==1.4.15

# --- Data handling / analysis ---
pandas==2.2.3

# --- Plotting ---
matplotlib==3.9.2

# --- Notebooks ---
jupyter==1.1.1
nbformat==5.10.4
nbclient==0.10.0
